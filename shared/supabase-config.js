/* =============================================================
   NOSTALGIA.EXE — Supabase configuration
   -------------------------------------------------------------
   Paste your project's URL and anonymous (public) key below.
   These are safe to ship to the browser — they only grant
   access through the Row-Level Security policies you've set
   on the database. NEVER paste your *service role* key here.

   Get them from:
     Supabase Dashboard → Project → Settings → API
   ============================================================= */
window.SUPABASE_CONFIG = {
  url:     'https://vswumhfhvmpkzkjghvyy.supabase.co',
  anonKey: 'sb_publishable_0QDtYebXR44JaopgDRtjpw_GmgaUy_i',
};
